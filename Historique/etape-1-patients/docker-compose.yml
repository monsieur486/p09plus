services:

  ms-eureka:
    build:
      context: .
      dockerfile: ms-eureka/Dockerfile
    container_name: ms-eureka
    profiles: ["fullstack"]
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8761:8761"

  ms-gateway:
    build:
      context: .
      dockerfile: ms-gateway/Dockerfile
    container_name: ms-gateway
    profiles: ["fullstack"]
    depends_on:
      - ms-eureka
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "9000:9000"

  patients-db:
    image: postgres:16
    container_name: patients-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-patientsdb}
      POSTGRES_USER: ${POSTGRES_USER:-patient_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-patient_pass}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  ms-patients:
    build:
      context: .
      dockerfile: ms-patients/Dockerfile
    profiles: ["fullstack"]
    depends_on:
      - ms-eureka
      - patients-db
      - ms-gateway
    environment:
      SPRING_PROFILES_ACTIVE: docker
      APP_USERNAME: ${APP_USERNAME:-app_user}
      APP_PASSWORD: ${APP_PASSWORD:-app_password}

  ms-webclient:
    build:
      context: .
      dockerfile: ms-webclient/Dockerfile
    profiles: ["fullstack"]
    container_name: ms-webclient
    depends_on:
      - ms-patients
    environment:
      SPRING_PROFILES_ACTIVE: docker
      APP_USERNAME: ${APP_USERNAME:-app_user}
      APP_PASSWORD: ${APP_PASSWORD:-app_password}
    ports:
      - "${PORT_WEBCLIENT:-8080}:8080"

volumes:
  db_data:
  database-data:
